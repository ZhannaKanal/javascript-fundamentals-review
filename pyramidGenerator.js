const pyramid = (strToRepeat, numOfRows, isFalse)=>{
    let result = "\n";
    if (!isFalse) {
        for (let i = 0; i < numOfRows; i++){}
    } else {
        for (let i = 0; i < numOfRows; i++){}
    }
}